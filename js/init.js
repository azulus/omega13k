const _ = {};

const $ = {
  USABLE_COLORS: '456789abcdef',

  assign: Object.assign,
  abs: Math.abs,
  floor: Math.floor,
  round: Math.round,
  pow: Math.pow,
  document: document,
  parseFloat: parseFloat,

  splitString: (s) => s.split(''),
};
